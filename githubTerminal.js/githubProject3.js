working on my local branch
ready to commit
git add .
git commit -m ''
now its up to date on my local branch
git checkout master
this switches to master so i can merge
git merge localbranchname
now my LOCAL master and local branch are merged and i need to push it to GitHub Origin master
git pull origin master


if i'm behind the commits i need to
git push origin master
 i // option C // option C // :qu!
then i can
git push origin master


Create the branch on your local machine and switch in this branch :
$ git checkout -b [name_of_your_new_branch]
Change working branch :
$ git checkout [name_of_your_new_branch]
Push the branch on github :


GIT PULL ORIGIN DEVELOPMENT will get you the current branch
